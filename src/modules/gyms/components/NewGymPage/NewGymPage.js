import React from 'react'

import AddGym from '../AddGym'

const NewGymPage = ({
  handleFormSubmit
}) => (
  <div>
    <AddGym handleFormSubmit={handleFormSubmit} />
  </div>
)

export default NewGymPage
